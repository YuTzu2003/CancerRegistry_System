# 系統介紹

為院內癌症登記資料的處理與管理平台。它的目標不是取代癌症登記規範、臨床判斷或人工審核，而是將格式管理、資料清洗、品質檢核、分析與作業記錄集中，讓不同角色依權限完成各自的工作。

## 服務對象

| 對象 | 使用情境 |
| --- | --- |
| 癌症登錄人員 | 上傳登錄資料、清洗與檢核資料、修正日期錯誤、下載處理結果、查看個人歷程。 |
| 資料管理人員 | 管理資料格式、欄位對照、組織學代碼、全國資料及版本。 |
| 分析與報告人員 | 上傳分析資料、製作儀表板、比較年度資料、產出報告與 LLM 輔助敘述。 |
| 管理者 | 審核 Key 申請、管理成員與權限、查看稽核紀錄、設定 Power BI 發布資料。 |
| 院內資訊人員 | 維護 Windows、IIS、SQL Server、模型供應商、備份、日誌與服務健康狀態。 |

## 網頁架構

系統採用 Web 視覺化操作介面，左側配置功能導覽列（Sidebar），依使用者權限動態呈現對應之頁面與功能模組：

```text
首頁 ── 最新公告、常用下載與系統概況
│
├── 資料審核
│   ├── 資料清洗 ──────── 格式整理、日期防錯校驗、DQI 品質指標與成果下載
│   └── 資料審核紀錄 ── 個人作業歷程、明細檢視與批次下載
│
├── 報表分析
│   ├── 年報分析 ──── 多維度統計、圖表分析、LLM 智慧敘述與報告產出
│   ├── 年度比較 ────── 跨年度／跨資料集對比與差異分析
│   └── 資料維護 ── 金鑰登入、全國資料、組織學代碼與版本管理
│
├── 虛擬資料生成 ───── 合成癌症登記測試資料與模擬資料集產製
│
├── 監測指標 ────── 癌症登記品質監測指標規則檢核與統計
│
├── 權限申請 ── 金鑰存取權限申請與審核進度查詢
│
└── 權限管理
    ├── 使用者管理 ───── 帳號啟用／停用與管理員角色指派
    ├── 金鑰申請審核 ── 審核、核發與撤銷資料維護金鑰
    └── 系統操作日誌 ──── 登入紀錄、資料異動與資安稽核軌跡
```

## 系統部署架構

正式環境採用 Windows 伺服器架構，以 **IIS 反向代理 + ARR 負載分流 + Waitress 多程序 WSGI + 獨立 LLM Worker + 既有 SQL Server** 進行多層分流部署：

```text
使用者 (瀏覽器)
    │
    ▼ HTTP/HTTPS (預設 Port 5000 / 443)
┌────────────────────────────────────────────────────────┐
│ IIS 站台 (CancerRegistrySystem)                         │
│ ├─ 靜態資源 (/static/) ── 快取 7 天直接回應             │
│ └─ 動態請求 ── URL Rewrite 反向代理至 ARR Server Farm   │
└──────────────────────────┬─────────────────────────────┘
                           │ ARR 負載平衡分流
                           ▼
┌────────────────────────────────────────────────────────┐
│ Waitress Web Worker (Loopback 127.0.0.1:51001~51008)   │
│ └─ Flask 應用程式 ── 業務邏輯處理與工作階段管理         │
└──────────────┬──────────────────────────┬──────────────┘
               │ 讀寫操作                  │ 任務派送 / 狀態查詢
               ▼                          ▼
┌──────────────────────────────┐ ┌───────────────────────┐
│ Microsoft SQL Server         │ │ 獨立 LLM Worker 程序  │
│ ├─ 使用者與權限 (RBAC)        │ │ (與 Web 服務程序隔離) │
│ ├─ 癌症登記與全國資料庫       │ └──────────┬────────────┘
│ ├─ 稽核日誌 (Audit Logs)     │            │ API 呼叫
│ └─ LLM 非同步任務佇列         │            ▼
└──────────────────────────────┘ ┌───────────────────────┐
                                 │ Ollama 或 OpenAI API  │
                                 └───────────────────────┘
```

- **流量分流與資安防護**：IIS 負責對外連線、HTTPS 憑證終結與靜態資源快取；後端 Waitress 僅監聽 `127.0.0.1` 本機埠，禁止外部直接存取。
- **運算隔離保證響應**：生成式 AI（LLM）分析任務由獨立背景 Worker 處理，長時間的模型推理運算不會阻塞 Web 執行緒，確保一般登錄與審核作業流暢。
- **既有資料庫無縫介接**：直接連線院內既有 SQL Server，部署過程不更動或刪修既有資料表結構。

## 功能總覽

### 帳號、權限與稽核

- 使用者登入、登出、個人資料及密碼變更。
- Key 申請、核准、拒絕、停用與重新啟用。
- 成員管理及管理者權限控管。
- 登入與重要異動稽核紀錄；登入失敗次數過多時暫時限制嘗試。

### 資料格式與參照資料管理

- 建立、編修與管理資料格式版本。
- 管理欄位對照及欄位分類設定。
- 管理組織資料存取權限。
- 管理組織學代碼：新增、編修、Excel 匯入、批次刪除、版本預覽及還原。
- 管理全國資料：匯入、編修、刪除、版本儲存、預覽及還原。

### 資料清洗與品質檢核

- 上傳登錄資料並依選定格式進行整理、轉換與檢核。
- 預覽資料、分類欄位、調整格式及修正日期錯誤。
- 產出清洗結果、品質報告與下載檔案。
- 保存個人作業歷程，支援查看明細、單筆／批次下載及刪除。
- 顯示完整性、正確性、一致性及 DQI 等品質資訊。

### 資料產製與指標監測

- 分析上傳檔案的欄位和日期欄位。
- 依資料格式、欄位選擇、特殊設定及命名規則產製資料檔。
- 暫存產製檔案以使用者工作階段管理，逾時清理。
- 上傳指標資料、預覽內容並依監測規則處理。

### 儀表板、比較與報表

- 上傳 Excel 分析資料、預覽資料欄位及年度範圍。
- 依癌別、年度、性態碼與分期條件分析資料。
- 建立年度分析與跨年度／跨檔案比較。
- 儲存個人常用篩選條件。
- 產生圖表、年度報告、比較報告與匯出檔。
- 管理者可設定 Power BI 發布路徑並匯出資料。

### LLM 輔助分析

- 支援 Ollama 或 OpenAI 產生圖表與比較敘述。
- LLM 任務會依登入使用者建立、查詢、取消與重試；一般使用者不能讀取其他人的任務。
- 正式環境的 LLM 任務由獨立 worker 處理，避免長時間模型請求佔用 Web 程序。
- 使用 LLM 前應完成院內資料治理、模型供應商、網路出口與敏感資料使用審查。

### 首頁資訊

- 顯示系統統計摘要。
- 定期取得並顯示台灣癌症登記相關公告與下載資訊。

## 資料與權限原則

- 資料庫中的使用者、格式、作業與稽核資料為系統主要紀錄。
- 每位使用者只能管理其授權範圍內的作業、上傳檔案及 LLM 任務。
- 管理者功能以既有權限檢查保護，管理動作應保留稽核資訊。
- `.env`、日誌與文件不得包含資料庫密碼、API key、病歷識別資訊或完整 LLM prompt／response。

## 系統範圍

本系統處理癌症登記資料的管理、整理、品質檢核及分析輔助。資料是否符合最終申報或醫療判定，仍須依最新癌症登記規範、院內流程及授權人員審核。

## 相關文件

- [README.md](README.md)：系統定位、概略功能與部署架構。
- [架設步驟.md](架設步驟.md)：完整 Windows 正式部署與維運流程。
- [deploy/README.md](deploy/README.md)：部署指令快速索引。
