# 癌症登記資料平台：正式環境架設手冊

本手冊適用於 Windows Server／Windows 10、11，以 IIS + ARR + Waitress 執行癌症登記資料平台，資料庫使用 Microsoft SQL Server。

## 1. 部署原則

若同一台主機已經有其他 Flask 系統，兩套系統可以並存，但必須各自使用：

- 獨立的專案資料夾與 Python 虛擬環境。
- 獨立的 SQL Server 資料庫；不得共用既有系統資料庫。
- 不同的 IIS 對外連接埠，或各自的 HTTPS 網域。
- 不同的 Waitress 內部連接埠。

本專案預設會建立 IIS Site `CancerRegistrySystem`、ARR Server Farm `CancerRegistryBackend`、Windows 排程工作 `CancerRegistrySystem-*`。若這些名稱已被其他癌登環境使用，請先停下來確認，不要直接覆蓋。

## 2. 架構與服務

```text
使用者瀏覽器
    -> IIS + URL Rewrite + ARR
    -> Waitress（僅監聽 127.0.0.x）
    -> Flask
    -> SQL Server

Dashboard 的 LLM 工作由獨立 Windows 排程工作執行。
```

IIS 對外提供網站與靜態檔案；Waitress 不應直接對外開放。

## 3. 先決條件

在伺服器安裝或準備下列項目：

- Git、Python 3.12、`uv`。
- SQL Server 與 ODBC Driver 17 或 18 for SQL Server。
- IIS 與 IIS Management Tools。
- IIS URL Rewrite 2.1、IIS Application Request Routing 3.0（ARR）。
- LLM Provider：Ollama 或 OpenAI API。
- 如需產生 PDF，伺服器必須能下載 Playwright Chromium。
- 如需自行忘記密碼，必須準備 Gmail 寄件帳號與應用程式密碼。

以系統管理員身分開啟 PowerShell，安裝 IIS：

```powershell
# Windows Server
Install-WindowsFeature Web-Server, Web-Mgmt-Tools

# Windows 10/11
Enable-WindowsOptionalFeature -Online -FeatureName IIS-WebServerRole -All -NoRestart
Enable-WindowsOptionalFeature -Online -FeatureName IIS-ManagementConsole -All -NoRestart
```

URL Rewrite 與 ARR 必須另外安裝。完成後可在專案目錄驗證：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\prepare-iis.ps1
```

## 4. 第一次安裝

### 4.1 下載程式

以下示例將系統安裝至 `D:\CancerRegistry`。不要安裝到既有 Notebook-Flask 專案目錄。

```powershell
Set-Location D:\
git clone https://github.com/YuTzu2003/CancerRegistry_System.git CancerRegistry
Set-Location D:\CancerRegistry
uv sync
Copy-Item .env.example .env
```

### 4.2 建立專用、空白的資料庫

以 SQL Server 管理者帳號建立新的資料庫，例如：

```sql
CREATE DATABASE CancerRegistry;
```

首次初始化只接受沒有任何使用者資料表的資料庫。不要將 Notebook-Flask 或其他系統的連線字串填入癌登 `.env`。

部署帳號首次安裝至少需要建立資料表、索引與預存程序的權限。安裝完成後，可依院內規範降低權限。

### 4.3 設定 `.env`

用文字編輯器開啟 `.env`：

```powershell
notepad .env
```

正式環境至少調整下列設定。`SECRET_KEY` 可用 `uv run python -c "import secrets; print(secrets.token_hex(32))"` 產生。

```env
APP_ENV=production
APP_DEBUG=false
SECRET_KEY=請填入至少32字元的隨機字串

# 必須是癌登專用資料庫
SQLALCHEMY_DATABASE_URI=mssql+pyodbc://帳號:密碼@localhost:1433/CancerRegistry?driver=ODBC+Driver+17+for+SQL+Server&TrustServerCertificate=yes

# 若既有 Notebook-Flask 使用 5000，癌登可使用 5001
PUBLIC_HTTP_PORT=5001
BACKEND_BASE_PORT=51001
APP_WORKERS=1
LLM_WORKERS=1

WAITRESS_HOST=127.0.0.1
WAITRESS_THREADS=8
WAITRESS_BACKLOG=256
WAITRESS_CONNECTION_LIMIT=256
WAITRESS_CHANNEL_TIMEOUT=300
TRUSTED_PROXY=127.0.0.1
PROXY_COUNT=1

SESSION_COOKIE_SAMESITE=Lax
SESSION_LIFETIME_SECONDS=28800
```

正式環境應使用 IIS HTTPS binding，並設定：

```env
SESSION_COOKIE_SECURE=true
```

若仍暫時以純 HTTP 測試，才可暫時設為 `false`；不要將含病歷或帳號資料的正式系統長期維持 HTTP。

### 4.4 設定 LLM

選擇其中一種設定：

```env
# Ollama
LLM_PROVIDER=ollama
LLM_BASE_URL=http://ollama-server:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=gemma4:26b
LLM_TIMEOUT_SECONDS=180
```

```env
# OpenAI
LLM_PROVIDER=openai
OPENAI_API_KEY=請填入API金鑰
OPENAI_MODEL=gpt-4o-mini
LLM_TIMEOUT_SECONDS=180
```

`.env` 含有密碼與 API 金鑰，不得提交至 Git。

### 4.5 啟用 Email 綁定與忘記密碼（選用）

若要讓使用者透過 Email 收驗證碼並重設密碼，四項設定都必須完整：

```env
EMAIL_VERIFICATION_ENABLED=true
GMAIL_SMTP_ENABLED=true
GMAIL_SENDER_EMAIL=寄件Gmail帳號
GMAIL_SMTP_APP_PASSWORD=Gmail應用程式密碼
PASSWORD_CODE_MINUTES=10
VERIFICATION_CODE_RESEND_SECONDS=60
VERIFICATION_CODE_MAX_PER_HOUR=5
```

未啟用時，個人資訊頁會明確標示 Email 驗證服務停用，忘記密碼需由資訊室協助；這不是 Flask debug 模式。

### 4.6 執行首次部署

確認 `.env` 的資料庫名稱與連接埠無誤後，以系統管理員 PowerShell 執行：

```powershell
Set-Location D:\CancerRegistry
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\setup-and-deploy.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
```

首次部署會：

1. 安裝 Python 套件與 Playwright Chromium。
2. 對空白資料庫建立初始 schema 與初始資料。
3. 建立 IIS Site、ARR Server Farm、靜態檔案虛擬目錄與防火牆規則。
4. 建立並啟動 Waitress 與 LLM Windows 排程工作。
5. 檢查 Waitress、IIS、SQL Server 與 LLM 狀態。

完成後開啟 `http://伺服器IP:PUBLIC_HTTP_PORT/` 驗證。正式上線前應改為 HTTPS 網域。

## 5. 資料庫結構調整

`deploy\init_database.py` 僅供首次建立空白資料庫使用，不能拿來更新已有資料的正式資料庫。

日後若要新增欄位、資料表或預設資料，請自行透過 SQL Server Management Studio 或院內既有的資料庫變更流程執行 SQL。系統部署不會自動修改既有資料庫。

正式環境調整資料庫前請先備份，並先在測試環境驗證 SQL。不要對已有資料的資料庫再次執行 `setup-and-deploy.ps1`。

## 6. 後續程式更新

每次更新前先備份資料庫。完成程式碼更新後，以系統管理員 PowerShell 執行：

```powershell
Set-Location D:\CancerRegistry
git pull
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\deploy-production.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
```

更新程式不會修改既有資料庫；不需要、也不應再次執行 `setup-and-deploy.ps1`。

IIS 對靜態檔案設定七天快取。更新 CSS 或 JavaScript 後若畫面仍是舊版，先以 `Ctrl+F5` 強制重新整理瀏覽器。

## 7. 日常檢查與日誌

```powershell
# 完整健康檢查
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1

# Web worker 日誌
Get-Content .\tasks\logs\cancer-registry-51001.log -Tail 100

# LLM worker 日誌
Get-Content .\tasks\logs\cancer-registry-llm-01.log -Tail 100

# LLM Provider 檢查
.\.venv\Scripts\python.exe .\deploy\check_llm_readiness.py
```

`APP_WORKERS` 大於 1 時，各 worker 日誌連接埠依序為 `BACKEND_BASE_PORT`、`BACKEND_BASE_PORT + 1` 等。

## 8. 常見問題

### 新資料庫無法初始化

先確認 `.env` 實際連到新建資料庫，而非舊系統資料庫：

```powershell
.\.venv\Scripts\python.exe -c "from modules.services.db import get_engine; from sqlalchemy import text; e=get_engine(); c=e.connect(); print(c.execute(text('SELECT DB_NAME()')).scalar()); print(c.execute(text('SELECT COUNT(*) FROM sys.tables WHERE is_ms_shipped = 0')).scalar()); c.close()"
```

輸出的資料庫名稱必須正確，資料表數量必須為 `0` 才能執行首次初始化。

### IIS 顯示 502

先執行健康檢查，再查看對應 Web worker 日誌。通常是 Waitress 沒有啟動、SQL Server 連線失敗，或 `.env` 的連接埠與 IIS 設定不一致。

### 忘記密碼無法寄信

執行 `check-production.ps1`。輸出 `Email verification: disabled` 代表兩個開關仍為 `false`；若顯示設定不完整，請補上寄件 Gmail 與應用程式密碼。勿將該密碼寫入 Git。

### 資料庫欄位或資料需要更新

不要重跑首次初始化。先備份資料庫，在測試環境驗證 SQL 後，再由資料庫管理人員對正式資料庫執行新增欄位或資料的 SQL。

## 9. 停止或解除自動啟動

停止癌登 Web 與 LLM 工作：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\stop-production.ps1
```

移除癌登的開機自動啟動排程工作：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\unregister-autostart.ps1
```

這兩項操作不會刪除 IIS Site、SQL Server 資料庫或既有資料。資料庫回復應使用已驗證的 SQL Server 備份程序。
