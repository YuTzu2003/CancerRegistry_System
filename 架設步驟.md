# CancerRegistry_System 完整架設與維運步驟

本文件說明如何在 Windows Server 或 Windows 10/11 上，以 IIS、ARR、Waitress 與既有 SQL Server 資料庫部署 CancerRegistry_System。所有命令均應在專案根目錄執行；需要變更 IIS、防火牆或 Windows 排程工作時，PowerShell 必須以系統管理員身分開啟。

## 部署指令快速索引

在專案根目錄、且以系統管理員 PowerShell 執行：

```powershell
# 準備 IIS、URL Rewrite 與 ARR 前置檢查
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\prepare-iis.ps1

# 既有資料庫的正式部署
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\setup-and-deploy.ps1

# 檢查 IIS、Waitress 與 LLM worker
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1

# 停止服務或移除開機自啟工作
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\stop-production.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\unregister-autostart.ps1
```

> **注意事項**：部署不會執行資料庫 schema SQL。正式 `.env` 必須設定 `APP_ENV=production`、`APP_DEBUG=false`；現有 LLM task schema 的正式部署使用 `LLM_WORKERS=1`。

## 1. 部署範圍與架構

正式資料流如下：

```text
使用者 → IIS 站台 CancerRegistrySystem → ARR Server Farm CancerRegistryBackend
       → Waitress Web worker（僅 loopback）→ Flask → SQL Server

Flask → SQL Server LLM 任務 → 獨立 LLM worker → Ollama 或 OpenAI
```

- IIS 直接回應 `/static/`，瀏覽器快取期限為 7 天。
- IIS 對外使用 `.env` 的 `PUBLIC_HTTP_PORT`；預設為 `5000`。
- Waitress 僅監聽 `127.0.0.1` 至 `127.0.0.8` 的連續後端埠，預設從 `51001` 開始；不建立外部防火牆規則。
- Web worker 與 LLM worker 是不同程序。現有既有資料庫部署使用一個 LLM worker（`LLM_WORKERS=1`）。
- 部署腳本不會執行 `deploy/database/CancerRegistry_System.sql`，也不會建立、刪除或修改資料庫 schema。

## 2. 部署前確認

部署前逐項確認，任一項不成立先處理完成再進行：

1. 已取得既有 SQL Server 資料庫的可還原備份，並確認 `.env` 指向正確資料庫。
2. `.env` 已存在且未被 Git 追蹤。
3. 有系統管理員 PowerShell 權限。
4. 已確認 `PUBLIC_HTTP_PORT` 未被其他 IIS 站台使用，且 `BACKEND_BASE_PORT` 起的連續埠未被其他程序使用。
5. 已確認 SQL Server 連線帳號、TLS 憑證與 ODBC Driver 相容；請使用受信任憑證或正確的 `TrustServerCertificate` 設定，勿以關閉加密替代。
6. 已確認 LLM 使用 Ollama 或 OpenAI，並完成院內資料治理、模型使用及 API key 管理程序。
7. 若院內強制 HTTPS，已準備憑證與 IIS binding 資訊。

檢查 Git 與連接埠：

```powershell
git status --short
Get-NetTCPConnection -State Listen | Where-Object LocalPort -in 5000,51001,51002,51003,51004,51005,51006,51007,51008
```

## 3. 系統需求

### 軟體

| 項目 | 用途 |
| --- | --- |
| Git | 取得及更新程式碼。 |
| Python 3.12 與 uv | 建立虛擬環境與安裝套件。 |
| SQL Server | 系統既有資料庫。 |
| ODBC Driver 17 或 18 for SQL Server | Python 連線 SQL Server。 |
| IIS 與 Management Tools | 對外 Web 站台與管理工具。 |
| IIS URL Rewrite 2.1 | 反向代理 rewrite 規則。 |
| IIS Application Request Routing 3.0 | ARR Server Farm 與後端負載分流。 |
| Ollama 或 OpenAI | LLM 輔助分析的模型供應商。 |

Windows Server 安裝 IIS：

```powershell
Install-WindowsFeature Web-Server, Web-Mgmt-Tools
```

Windows 10/11 安裝 IIS：

```powershell
Enable-WindowsOptionalFeature -Online -FeatureName IIS-WebServerRole -All -NoRestart
Enable-WindowsOptionalFeature -Online -FeatureName IIS-ManagementConsole -All -NoRestart
```

接著安裝 URL Rewrite 2.1 與 ARR 3.0。完成後，IIS Manager 的伺服器節點應出現 **URL Rewrite**、**Application Request Routing Cache** 及 **Server Farms**。

### 硬體與容量

| 分類 | 最低值 | 起始建議 | 壓力測試後調整 |
| --- | --- | --- | --- |
| Web 伺服器 | 4 CPU 核心、8 GB RAM | 8 CPU 核心、16 GB RAM、SSD | 依 Web 同時連線、上傳檔案大小與 SQL 延遲調整 `APP_WORKERS` 與 Waitress threads。 |
| SQL Server | 依既有資料庫負載 | 與 Web 分離，使用備份與監控 | 依查詢延遲、I/O、記憶體與備份時間調整。 |
| LLM | 可連至已部署模型 | 使用獨立 Ollama 主機或受控 OpenAI 配額 | 記錄 queue 深度、工作延遲、429／5xx 與模型資源後再評估擴充。 |

以上為部署起點，不代表已量測的可支援使用者數。

## 4. 下載或更新程式

首次部署：

```powershell
Set-Location D:\YLH
git clone https://github.com/YuTzu2003/CancerRegistry_System.git
Set-Location D:\YLH\CancerRegistry_System
uv sync
```

既有機器更新：

```powershell
Set-Location D:\YLH\CancerRegistry_System
git pull
uv sync
```

## 5. 確認既有 SQL Server 資料庫

本次部署假設資料庫已由院內流程建立完成。請勿執行 `deploy/init_database.py`，也不要直接修改 `data/Hospital_data.bak`。

使用 `.env` 的連線設定做唯讀連線測試：

```powershell
.\.venv\Scripts\python.exe -c "from modules.services.db import get_engine; get_engine().connect().close(); print('SQL Server connection: OK')"
```

若失敗，請先確認：SQL Server 主機與埠、帳號權限、ODBC Driver 版本、TLS 憑證與連線字串。不應在連線失敗時繼續部署 IIS。

## 6. 建立正式環境設定

首次建立 `.env`：

```powershell
Copy-Item .env.example .env
notepad .env
```

正式環境至少應包含以下設定；資料庫主機、帳號、密碼、模型與 API key 請填入院內實際值，勿將這些值寫入文件或提交 Git：

```ini
APP_ENV=production
APP_DEBUG=false
SECRET_KEY=至少32字元的隨機字串
SQLALCHEMY_DATABASE_URI=mssql+pyodbc://帳號:密碼@SQL主機:1433/資料庫名稱?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes

SESSION_COOKIE_SECURE=false
SESSION_COOKIE_SAMESITE=Lax
SESSION_LIFETIME_SECONDS=28800

FLASK_HOST=127.0.0.1
FLASK_PORT=5000
PUBLIC_HTTP_PORT=5000
BACKEND_BASE_PORT=51001
APP_WORKERS=1

WAITRESS_HOST=127.0.0.1
WAITRESS_THREADS=8
WAITRESS_BACKLOG=256
WAITRESS_CONNECTION_LIMIT=256
WAITRESS_CHANNEL_TIMEOUT=300
TRUSTED_PROXY=127.0.0.1
PROXY_COUNT=1

LLM_WORKERS=1
LLM_PROVIDER=ollama
LLM_BASE_URL=http://ollama-host:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=模型名稱
LLM_TIMEOUT_SECONDS=180
```

使用 OpenAI 時，設定 `LLM_PROVIDER=openai`，並填入 `OPENAI_API_KEY` 與 `OPENAI_MODEL`；不要在 `.env.example`、README、日誌或截圖放入真正的 API key。

產生密鑰：

```powershell
uv run python -c "import secrets; print(secrets.token_hex(32))"
```

注意事項：

- 資料庫密碼若包含 `@`、`:`、`/`、`?`、`#` 等符號，必須 URL 編碼。
- `APP_WORKERS` 可設定為 1 到 8；只有壓力測試後才提高。
- `LLM_WORKERS` 在目前既有資料庫部署必須維持 `1`。
- 尚未設定 HTTPS 時，`SESSION_COOKIE_SECURE=false`；HTTPS binding 完成後再改成 `true`。
- `.env` 不可提交 Git，且應限制只有服務帳號與管理人員可讀取。

## 7. 執行正式部署

以系統管理員 PowerShell 執行：

```powershell
Set-Location D:\YLH\CancerRegistry_System
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\setup-and-deploy.ps1
```

部署依序執行：

1. 驗證系統管理員權限、uv、ODBC Driver、`.env` 與 production 安全設定。
2. 執行 `uv sync`，並驗證 SQL Server 連線。
3. 檢查 IIS、URL Rewrite、ARR 並啟用 ARR proxy。
4. 建立或更新 `CancerRegistrySystem` IIS 站台、`CancerRegistryBackend` Server Farm、靜態檔案對應與公開 HTTP 防火牆規則。
5. 註冊並啟動 `CancerRegistrySystem-01` 起的 Waitress 工作，以及 `CancerRegistrySystem-LLM-01` LLM 工作。
6. 檢查每個 Waitress `/health`、IIS 公開 `/health`、SQL Server 與 LLM provider readiness。

成功後，使用下列網址測試：

```text
http://伺服器名稱或IP:PUBLIC_HTTP_PORT/
```

若前置檢查、SQL 連線或 IIS 設定失敗，腳本會停止並保留錯誤訊息；不得將該次執行視為完成部署。

## 8. HTTPS 設定

若院內規定使用 HTTPS，請先在 IIS 為 `CancerRegistrySystem` 建立有效憑證 binding，再將：

```ini
SESSION_COOKIE_SECURE=true
```

寫入 `.env`，重新執行部署。確認 HTTPS 正常後，依院內規範設定 HTTP 至 HTTPS 轉址。未完成 HTTPS binding 前不可將 Secure cookie 打開，否則使用者無法維持登入 session。

## 9. 部署後驗收

本機健康檢查：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
```

從另一台院內電腦驗收：

1. 以公開網址開啟登入頁並登入。
2. 確認 CSS、JavaScript 與圖示等靜態資源可正常載入。
3. 上傳一份非敏感測試資料，確認資料清洗、下載或核心資料處理流程正常。
4. 確認儀表板資料上傳、分析與比較功能可用。
5. 建立一筆 LLM 測試任務，確認任務狀態、結果與使用者隔離正常；測試資料不可含病歷識別資訊。
6. 確認非管理者無法存取成員、權限與稽核管理功能。

重開機驗收：

1. 重新啟動 Windows，且不登入一般使用者帳號。
2. 等待 SQL Server、排程工作與 IIS 啟動完成。
3. 從另一台電腦再次測試公開網址與登入。
4. 執行 `check-production.ps1`，確認 Web 與 LLM 排程工作存在並正常。

## 10. 日常更新

更新前先確認工作樹與備份狀態：

```powershell
Set-Location D:\YLH\CancerRegistry_System
git status --short
git pull
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\deploy-production.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
```

部署腳本不會執行 schema SQL。若版本更新需要資料庫 migration，應先閱讀 migration 的資料影響、建立可還原備份並取得院內核准後另行執行。

## 11. 日誌、備份與監控

| 項目 | 位置或做法 |
| --- | --- |
| Waitress 日誌 | `tasks\logs\cancer-registry-<port>.log` |
| LLM worker 日誌 | `tasks\logs\cancer-registry-llm-01.log` |
| IIS 日誌 | IIS 預設 log 目錄或院內指定位置。 |
| 稽核資料 | SQL Server 的 `Audit_logs`。 |
| 資料庫備份 | 依院內 SQL Server 備份政策執行，部署前後應確認可還原。 |

建議定期檢查公開健康檢查、Windows 排程最後執行結果、SQL Server 連線失敗、IIS 5xx、LLM 任務失敗率與佇列等待時間。日誌不得寫入密碼、API key、完整 prompt 或病歷識別資料。

查看最近日誌：

```powershell
Get-Content .\tasks\logs\cancer-registry-51001.log -Tail 100
Get-Content .\tasks\logs\cancer-registry-llm-01.log -Tail 100
```

## 12. 停止、解除與回復

停止 Web 與 LLM 排程工作：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\stop-production.ps1
```

移除本專案的開機自啟工作：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\unregister-autostart.ps1
```

上述腳本只操作 `CancerRegistrySystem*` 命名範圍，不刪除 IIS 站台、SQL Server 資料、使用者檔案、`tasks\Jobs` 或備份檔。

若需回復程式碼，使用已驗證的 Git commit 或明確檔案版本；不要使用 `git reset --hard`。資料庫回復則依院內 SQL Server 備份／還原流程執行。

## 13. 常見問題

### IIS、URL Rewrite 或 ARR 缺少

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\prepare-iis.ps1
```

依錯誤訊息安裝缺少元件後重新執行。請勿手動覆蓋其他 IIS 站台或 Server Farm。

### SQL Server 連線失敗

確認 `.env` 的主機、埠、資料庫名稱、帳號權限、ODBC Driver 與 TLS 設定。可執行第 5 節的唯讀連線測試；修正後再部署。

### Waitress 健康檢查失敗

```powershell
Invoke-RestMethod http://127.0.0.1:51001/health
Get-Content .\tasks\logs\cancer-registry-51001.log -Tail 100
```

請依 `.env` 的 `BACKEND_BASE_PORT` 調整埠號，並確認 SQL Server 可連線。

### IIS 公開入口無法連線

```powershell
Invoke-RestMethod http://127.0.0.1:5000/health
```

將 `5000` 改成 `PUBLIC_HTTP_PORT`。再檢查 IIS site binding、防火牆規則、ARR Server Farm 狀態及院內網路 ACL。

### LLM readiness 失敗

確認 `LLM_PROVIDER`、模型名稱、Ollama endpoint 或 OpenAI API key。Ollama 需確認模型已下載；OpenAI 請確認帳號權限與網路出口。不要以實際病歷資料測試模型連線。

## 14. 開發模式

開發模式只供本機除錯使用，不應暴露至院內網路：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\start-development.ps1
```

| 用途 | `APP_ENV` | `APP_DEBUG` | 啟動方式 |
| --- | --- | --- | --- |
| 本機開發 | `development` | `true` | `uv run app.py` 或 `start-development.ps1` |
| IIS 正式服務 | `production` | `false` | `setup-and-deploy.ps1` 或 `deploy-production.ps1` |

正式模式一律關閉 Flask debugger，並透過 IIS／ARR 轉送請求。
