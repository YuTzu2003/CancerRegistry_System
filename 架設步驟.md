# CancerRegistry_System 架設步驟

本文件適用於 Windows Server 或 Windows 10/11 的正式環境。正式架構為：

`使用者 → IIS + ARR → Waitress → Flask → SQL Server`

IIS 直接提供 `/static/` 檔案並設定 7 天快取；其餘請求由 ARR 分流到一個或多個 Waitress 程序。Windows 重開機後，排程工作會自行啟動 Waitress，不需要使用者登入。

## 1. 前置需求

請以系統管理員身分完成下列安裝：

| 項目 | 用途 |
| --- | --- |
| Git | 下載及更新程式碼 |
| uv 與 Python 3.12 | 建立虛擬環境及安裝 Python 套件 |
| SQL Server | 系統資料庫 |
| ODBC Driver 17 或 18 for SQL Server | Python 連線 SQL Server |
| IIS 與 Management Tools | 對外網站服務 |
| IIS URL Rewrite 2.1 | URL Rewrite 規則 |
| IIS Application Request Routing 3.0 | 反向代理與多程序分流 |

Windows Server 安裝 IIS：

```powershell
Install-WindowsFeature Web-Server, Web-Mgmt-Tools
```

Windows 10/11 安裝 IIS：

```powershell
Enable-WindowsOptionalFeature -Online -FeatureName IIS-WebServerRole -All -NoRestart
Enable-WindowsOptionalFeature -Online -FeatureName IIS-ManagementConsole -All -NoRestart
```

URL Rewrite 與 ARR 必須依序安裝。完成後，在 IIS Manager 伺服器節點應可看到 **URL Rewrite**、**Application Request Routing Cache** 與 **Server Farms**。

## 2. 下載程式與安裝套件

```powershell
Set-Location D:\YLH
git clone https://github.com/YuTzu2003/CancerRegistry_System.git
Set-Location D:\YLH\CancerRegistry_System
uv sync
```

如果是既有專案更新，改用：

```powershell
git pull
uv sync
```

## 3. 建立 SQL Server 資料庫

建立一個**新的空白資料庫**及專用 SQL Login。首次部署會執行：

```text
deploy\database\CancerRegistry_System.sql
```

SQL 腳本沒有固定資料庫名稱；實際目標由 `.env` 的 `SQLALCHEMY_DATABASE_URI` 決定。請確認該連線字串指向正確的新資料庫。

首次部署帳號需要建立資料表、索引與約束的權限。完成初始化後，建議改用只保留系統執行所需權限的帳號。

## 4. 建立正式環境設定

```powershell
Copy-Item .env.example .env
notepad .env
```

正式環境至少設定：

```ini
APP_ENV=production
APP_DEBUG=false
SECRET_KEY=請填入至少32字元的隨機字串
SQLALCHEMY_DATABASE_URI=mssql+pyodbc://帳號:密碼@SQL主機:1433/資料庫名稱?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes

PUBLIC_HTTP_PORT=5000
BACKEND_BASE_PORT=51001
APP_WORKERS=1
WAITRESS_THREADS=8
TRUSTED_PROXY=127.0.0.1
PROXY_COUNT=1
SESSION_COOKIE_SECURE=false
SESSION_COOKIE_SAMESITE=Lax
```

產生 `SECRET_KEY`：

```powershell
uv run python -c "import secrets; print(secrets.token_hex(32))"
```

注意事項：

- 資料庫密碼含 `@`、`:`、`/`、`?`、`#` 等符號時，必須進行 URL 編碼。
- `PUBLIC_HTTP_PORT` 是使用者對外連線的埠；不要與其他 IIS 站台重複。
- `BACKEND_BASE_PORT` 及後續連續埠只供本機 IIS/ARR 使用，不需開放防火牆。
- `APP_WORKERS=1` 是預設值。確認 CPU、記憶體與 SQL Server 負載後，才提高至 2 到 8。
- 未設定 HTTPS 時，`SESSION_COOKIE_SECURE` 必須維持 `false`。完成 HTTPS IIS binding 後才改為 `true`。
- `.env` 不可提交到 Git。

## 5. 第一次一鍵部署

以**系統管理員身分**開啟 PowerShell：

```powershell
Set-Location D:\YLH\CancerRegistry_System
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\setup-and-deploy.ps1
```

此命令依序執行：

1. 驗證 IIS、URL Rewrite、ARR、ODBC Driver 與 production `.env`。
2. 執行 `uv sync`。
3. 對 `.env` 指向的空白資料庫執行 schema SQL。
4. 建立 `CancerRegistrySystem` IIS 站台及 `CancerRegistryBackend` ARR Server Farm。
5. 將 `/static/` 對應到專案靜態目錄並設定 7 天快取。
6. 建立公開埠的防火牆規則。
7. 註冊 `CancerRegistrySystem-01` 起的 Windows 開機工作。
8. 啟動每個 Waitress worker，並檢查每個 worker 與 IIS 公開 `/health`。

成功後以：

```text
http://伺服器IP:PUBLIC_HTTP_PORT/
```

開啟網站。

## 6. 後續更新部署

程式更新後，使用下列命令。它不會再次執行 schema SQL：

```powershell
Set-Location D:\YLH\CancerRegistry_System
git pull
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\deploy-production.ps1
```

若需要手動啟動單一 production Waitress：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\start-production.ps1
```

## 7. Debug 與正式模式切換

開發／debug 模式：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\start-development.ps1
```

此命令只在目前 PowerShell 工作階段設定：

```ini
APP_ENV=development
APP_DEBUG=true
```

不會改寫 `.env`。關閉程序後，正式部署仍使用 `.env` 中的 `APP_ENV=production`。

要手動切換 `.env` 時：

| 用途 | APP_ENV | APP_DEBUG | 啟動方式 |
| --- | --- | --- | --- |
| 本機開發 | `development` | `true` | `uv run app.py` 或 `start-development.ps1` |
| IIS 正式服務 | `production` | `false` | `setup-and-deploy.ps1` 或 `deploy-production.ps1` |

正式模式永遠強制關閉 Flask debugger。

## 8. 檢查、停止與重新開機驗收

服務檢查：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
```

停止 Waitress 工作：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\stop-production.ps1
```

移除開機自啟工作（不會刪除 IIS 站台或資料庫）：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\unregister-autostart.ps1
```

重新開機驗收：

1. 確認 IIS 站台 `CancerRegistrySystem` 為 Started。
2. 重新啟動 Windows，且不要登入使用者帳號。
3. 等待約 30 秒讓 SQL Server 與 Waitress 工作完成啟動。
4. 從另一台電腦開啟網站，並執行 `check-production.ps1`。

## 9. 常見問題

### IIS 或 ARR 尚未安裝

執行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\prepare-iis.ps1
```

依錯誤訊息安裝 IIS、URL Rewrite 或 ARR 後重試。

### Worker 健康檢查失敗

查看日誌：

```powershell
Get-Content .\tasks\logs\cancer-registry-51001.log -Tail 100
```

並確認：

```powershell
Invoke-RestMethod http://127.0.0.1:51001/health
```

實際埠號依 `.env` 的 `BACKEND_BASE_PORT` 為準。

### IIS 公開入口無法連線

確認公開健康檢查：

```powershell
Invoke-RestMethod http://127.0.0.1:5000/health
```

將 `5000` 改成 `.env` 的 `PUBLIC_HTTP_PORT`，並確認該埠沒有被其他 IIS 站台使用。

### 資料庫初始化失敗

首次初始化只支援空白資料庫。若資料庫已存在資料表，請確認連線字串是否指向錯誤資料庫；不要對既有正式資料庫重跑 `setup-and-deploy.ps1`。