# CancerRegistry_System 正式環境部署與維運手冊

本手冊說明 CancerRegistry_System 在 Windows Server 或 Windows 10/11 的正式環境部署、版本更新、重開機驗收與常見問題排除。

正式架構如下：

```text
使用者
  → IIS 網站 CancerRegistrySystem
  → ARR Server Farm CancerRegistryBackend
  → Waitress Web worker（僅 127.0.0.x）
  → Flask → SQL Server

Flask → SQL Server LLM 任務佇列 → 獨立 LLM worker → Ollama 或 OpenAI
```

所有會變更 IIS、防火牆或 Windows 排程工作的指令，都必須在「系統管理員 PowerShell」執行。

## 1. 先確認部署模式

| 情境 | 使用指令 | 資料庫影響 |
| --- | --- | --- |
| 第一次部署，目標資料庫完全空白 | `deploy\setup-and-deploy.ps1` | 建立 schema 與初始資料。 |
| 既有系統更新、修正部署腳本或重新部署 | `deploy\deploy-production.ps1` | 不建立、不刪除、不修改 schema。 |
| 部署或重開機後驗收 | `deploy\check-production.ps1` | 唯讀檢查。 |

> 已有資料表的資料庫不可執行 `setup-and-deploy.ps1`。它只支援全新空白資料庫，偵測到資料表後會停止。

## 2. 前置需求

- Windows Server 或 Windows 10/11（64 位元）。
- Git、Python 3.12 以上與 `uv`。
- Microsoft SQL Server，以及 ODBC Driver 17 或 18 for SQL Server。
- IIS 與 Management Tools。
- IIS URL Rewrite 2.1 與 IIS Application Request Routing 3.0（ARR）。
- 網際網路或可用的內部套件鏡像；首次部署需下載 Playwright Chromium，供 PDF 匯出使用。
- 可用的 Ollama 或 OpenAI LLM 服務。
- 正式環境資料庫的可還原備份與變更核准。

安裝 IIS：

```powershell
# Windows Server
Install-WindowsFeature Web-Server, Web-Mgmt-Tools

# Windows 10/11
Enable-WindowsOptionalFeature -Online -FeatureName IIS-WebServerRole -All -NoRestart
Enable-WindowsOptionalFeature -Online -FeatureName IIS-ManagementConsole -All -NoRestart
```

安裝 URL Rewrite 與 ARR 後，執行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\prepare-iis.ps1
```

成功時會顯示 `IIS, URL Rewrite, and ARR are ready.`。若缺少元件，請依腳本輸出的官方下載頁完成安裝後再執行。

## 3. 取得程式碼與設定環境

首次取得程式：

```powershell
Set-Location D:\CancerRegistry
git clone https://github.com/YuTzu2003/CancerRegistry_System.git _System
Set-Location D:\CancerRegistry\_System
git switch feature/deploy_systemV2
uv sync
```

建立正式環境設定：

```powershell
Copy-Item .env.example .env
notepad .env
```

`.env` 至少要設定下列值；不得提交 `.env`、資料庫密碼或 API key 至 Git。

| 設定 | 正式環境要求 |
| --- | --- |
| `APP_ENV` | `production` |
| `APP_DEBUG` | `false` |
| `SECRET_KEY` | 至少 32 字元的隨機值。 |
| `SQLALCHEMY_DATABASE_URI` | 指向正式 SQL Server 的 SQLAlchemy/ODBC 連線字串。 |
| `PUBLIC_HTTP_PORT` | IIS 對外連接埠，例如 `5000`。 |
| `BACKEND_BASE_PORT` | Waitress 起始連接埠，例如 `51001`。 |
| `APP_WORKERS` | `1` 至 `8`；首次建議 `1`。 |
| `LLM_WORKERS` | 必須是 `1`。 |
| `WAITRESS_HOST`、`TRUSTED_PROXY` | 都必須是 `127.0.0.1`。 |
| `PROXY_COUNT` | 必須是 `1`。 |
| `SESSION_COOKIE_SECURE` | HTTP 為 `false`；完成 HTTPS IIS binding 後改為 `true`。 |
| `LLM_PROVIDER` | `ollama` 或 `openai`。 |
| `LLM_TIMEOUT_SECONDS` | 單次 LLM 請求逾時秒數；預設 `180`。 |
| `GMAIL_SENDER_EMAIL` | 寄送帳號驗證碼的 Gmail 位址；未啟用 Email 功能時留空。 |
| `GMAIL_SMTP_APP_PASSWORD` | 寄件 Gmail 的應用程式密碼；不得提交至 Git。 |
| `GMAIL_SMTP_ENABLED`、`EMAIL_VERIFICATION_ENABLED` | 兩者皆設為 `true` 才啟用帳號 Email 綁定及忘記密碼驗證碼功能。 |
| `PASSWORD_CODE_MINUTES`、`VERIFICATION_CODE_RESEND_SECONDS`、`VERIFICATION_CODE_MAX_PER_HOUR` | 驗證碼有效分鐘數、重寄等待秒數及每小時寄送上限；預設為 `10`、`60`、`5`。 |

Email 功能使用 Gmail SMTP（`smtp.gmail.com:465`）；請填入已啟用兩步驟驗證的 Gmail 應用程式密碼，不可使用一般登入密碼。

產生 `SECRET_KEY`：

```powershell
uv run python -c "import secrets; print(secrets.token_hex(32))"
```

測試 SQL Server 連線。此步驟失敗時不可繼續部署 IIS：

```powershell
.\.venv\Scripts\python.exe -c "from modules.services.db import get_engine; get_engine().connect().close(); print('SQL Server connection: OK')"
```

## 4. 第一次部署：空白資料庫

確認 `.env` 已指向**空白**資料庫後執行：

```powershell
Set-Location D:\CancerRegistry\_System
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\setup-and-deploy.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
```

部署流程會：

1. 執行 `uv sync`、下載共用的 Playwright Chromium 與 SQL Server 連線檢查。
2. 執行 `deploy\init_database.py`，載入 `deploy\database\CancerRegistry_System.sql`。
3. 設定 IIS、URL Rewrite、ARR、靜態檔案與公開 HTTP 防火牆規則。
4. 建立或更新 IIS 網站 `CancerRegistrySystem` 與 ARR Server Farm `CancerRegistryBackend`。
5. 註冊並啟動 Web 與 LLM 的 Windows 開機排程工作。
6. 驗證 Waitress、IIS/ARR、SQL Server 與 LLM provider。

完成後，以 `http://伺服器名稱或IP:PUBLIC_HTTP_PORT/` 開啟系統。

## 5. 日常更新與重新部署：既有資料庫

程式更新、修正 IIS/LLM 啟動腳本或重新部署時，使用以下流程。此流程不會初始化資料庫。

```powershell
git pull

powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\deploy-production.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
```

若只更新 LLM worker 的 Python 或 PowerShell 腳本，完成 `git pull` 後可只重啟 LLM worker：

```powershell
Stop-ScheduledTask -TaskName CancerRegistrySystem-LLM-01
Start-ScheduledTask -TaskName CancerRegistrySystem-LLM-01
```

## 6. 開機自動啟動與重開機驗收

部署會建立以下 Windows 排程工作，皆以 `SYSTEM` 帳號、最高權限與 `AtStartup` 觸發：

| 排程工作 | 工作內容 |
| --- | --- |
| `CancerRegistrySystem-01` 至 `CancerRegistrySystem-08` | 啟動一個 Waitress Web worker。實際數量由 `APP_WORKERS` 決定。 |
| `CancerRegistrySystem-LLM-01` | 啟動單一 LLM worker。 |

Web worker 在 Windows 開機後等待 30 秒，再啟動 Waitress。LLM worker 會等待 SQL Server 可連線後才開始處理任務；每 5 秒重試一次。

重開機驗收：

1. 重開 Windows，但不要登入一般使用者帳號。
2. 等待至少 30 秒，讓 IIS 與 Waitress 完成啟動。
3. 從院內另一台電腦測試公開網址與登入。
4. 在伺服器執行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
```

## 7. Dashboard 檔案與 LLM 任務資料

| 類型 | 位置 | 說明 |
| --- | --- | --- |
| 新上傳 Dashboard Excel 檔 | `tasks\dashboard\file\<UUID>.xlsx` | 上傳的 `.xls`、`.xlsx` 檔案。既有舊路徑仍可讀取。 |
| 年報任務資料 | `tasks\dashboard\annual_report\<TaskID>` | `task.json`、`input.jsonl`、`output.jsonl` 與 `error.jsonl`。 |
| 比較報表任務資料 | `tasks\dashboard\comparison_report\<TaskID>` | 同上。 |
| 取消中的任務標記 | `tasks\dashboard\.cancelled` | 僅在刪除執行中任務時暫時出現；取消完成後會自動移除空資料夾。 |

不要手動刪除正在執行任務的資料夾或 `.cancelled` 內的標記，否則 worker 可能無法正常停止任務。

## 8. LLM 任務與日誌

正式環境只使用一個 LLM worker，因此任務會依序處理。年報與比較報表由多個圖表項目組成，每個項目會依序送至模型；單一項目的錯誤最多重試 3 次。

LLM 任務清單的「處理中」可能代表 `queued`、`running` 或 `retrying`。它不一定代表模型已開始生成；請以 LLM 排程與日誌判斷。

| 日誌 | 用途 |
| --- | --- |
| `tasks\logs\cancer-registry-<port>.log` | Web Waitress 日誌，例如登入、上傳與網站例外。 |
| `tasks\logs\cancer-registry-llm-01.log` | LLM worker 啟動、任務取得、完成與例外日誌。 |
| `tasks\dashboard\annual_report\<TaskID>\error.jsonl` | 單一年度報告任務的模型或處理失敗內容。 |

檢查 LLM worker：

```powershell
Get-ScheduledTask -TaskName CancerRegistrySystem-LLM-01 | Select-Object TaskName, State
Get-ScheduledTaskInfo -TaskName CancerRegistrySystem-LLM-01
Get-Content .\tasks\logs\cancer-registry-llm-01.log -Tail 100
.\.venv\Scripts\python.exe .\deploy\check_llm_readiness.py
```

若任務長時間沒有進度，先讀取 `cancer-registry-llm-01.log`。若 worker 已取得任務但模型沒有回應，檢查 Ollama/OpenAI 連線、模型名稱、模型資源與 `LLM_TIMEOUT_SECONDS`。不要把病歷識別資料貼入日誌、命令列或外部模型測試內容。

## 9. 常見問題

### IIS 顯示 502

502 表示 IIS/ARR 已啟動，但 Waitress 尚未能回應。先確認已等候開機後 30 秒，再執行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
Get-Content .\tasks\logs\cancer-registry-51001.log -Tail 100
Get-ScheduledTaskInfo -TaskName CancerRegistrySystem-01
```

將 `51001` 改為 `.env` 的 `BACKEND_BASE_PORT`。若 Web worker 未啟動，先確認 `.env` 的 `APP_ENV=production`、Python 虛擬環境與 SQL Server 設定。

### LLM 日誌沒有建立

確認讀取的檔案是 `cancer-registry-llm-01.log`，不是 Web worker 的 `cancer-registry-51001.log`。更新到含 LLM 啟動日誌的版本後，重啟 LLM worker：

```powershell
Stop-ScheduledTask -TaskName CancerRegistrySystem-LLM-01
Start-ScheduledTask -TaskName CancerRegistrySystem-LLM-01
Get-Content .\tasks\logs\cancer-registry-llm-01.log -Tail 100
```

### PDF 匯出顯示 Playwright Chromium 找不到

正式環境的 Web worker 由 `SYSTEM` 帳號啟動，不能使用管理員帳號個人設定檔內的 Playwright 瀏覽器。部署腳本會將 Chromium 安裝到專案共用位置 `tasks\playwright-browsers`，並由系統固定讀取該位置。

更新版本後重新部署即可補裝：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\deploy-production.ps1
```

若伺服器經由 Proxy 上網，先在同一個系統管理員 PowerShell 設定 Proxy，再執行部署：

```powershell
$env:HTTPS_PROXY = "http://proxy.example:8080"
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\deploy-production.ps1
```

### SQL Server 連線失敗

確認 `.env` 的主機、埠、資料庫、帳號、ODBC Driver 與 TLS 設定。修正後先執行第 3 節的 SQL 連線測試，再重新執行 `deploy-production.ps1`。

### IIS、URL Rewrite 或 ARR 缺少

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\prepare-iis.ps1
```

依腳本提示安裝缺少元件。不要手動覆蓋其他 IIS 網站或 ARR Server Farm。

## 10. 維護、停用與回復

停止 Web 與 LLM worker：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\stop-production.ps1
```

移除開機排程工作（不會刪除 IIS、SQL Server、使用者上傳檔或備份）：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\unregister-autostart.ps1
```

回復程式碼時，使用已驗證的 Git commit 或明確檔案版本。資料庫回復必須依院內 SQL Server 備份與還原程序處理；不要以 `git reset --hard` 取代資料庫回復。
